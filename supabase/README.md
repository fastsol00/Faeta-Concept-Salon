# Supabase Setup

1. Open Supabase SQL Editor.
2. Run `supabase/reset.sql`.
3. Add the environment variables from `.env.example`.
4. Start the app.

Default admin:

- Username: `admin`
- Password: `admin123`

Change the password from the admin settings after the first login.
